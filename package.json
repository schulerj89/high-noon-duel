{
  "name": "high-noon-duel",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "generate:audio": "node scripts/generate-local-audio.mjs",
    "generate:voices": "node scripts/generate-voiceovers.mjs"
  },
  "dependencies": {
    "three": "^0.170.0"
  },
  "devDependencies": {
    "@types/three": "^0.185.0",
    "typescript": "^5.6.3",
    "vite": "^8.1.0"
  }
}
